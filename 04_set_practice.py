s={1,2,3,'a','sagar'}
r={5,6,7,8,1}
#print(len(s))
#print(id(s))


#s.add(500)
#s.add(2)  # no effect on s because2 is already present
#s.update(r)   # update the s with r ,it will merge duplicate items
#print(s.union(r))# gives output dose not chane items of any set
#print(s.intersection(r)) # it gives common items in two sets
#print(s.difference(r))  # it gives uncommon items in s
#print(s.symmetric_difference(r))  # it gives uncommon items from both sets
#s.discard('sagar') # it wil discard specified item from set,if specified item not availble in set then do nothing
#s.remove('sagar')  # it removes specified item from set,it item is not available then return key error.
#s.pop() #  it removes random item from set.
#print(s.isdisjoint(r)) # return true if sets are intersection
#print(s.issubset(r)) # Report whether another set contains this set.
print(s)
print(r)
